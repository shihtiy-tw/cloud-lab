# cloud-lab Progress

**Last Updated**: 2026-08-18  
**Current Sprint**: Multi-Cloud Development VM (SPEC-003)

---

## Overall Status

**Active Work**: 0 in progress  
**Completed This Sprint**: SPEC-003 dev VM in all three clouds; SPEC-002 scripts

```text
AWS        [▓▓▓▓▓▓▓▓░░]  80% - 100 .tf files; dev-vm + VPC module added
GCP        [▓▓▓▓░░░░░░]  40% - First real Terraform: network module + dev-vm
Azure      [▓▓▓▓░░░░░░]  40% - First real Terraform: network module + dev-vm
Oracle     [▓░░░░░░░░░]  10% - Directory structure only, 0 .tf files
Tooling    [▓▓▓▓▓▓▓▓▓▓] 100% - 7 cloud.*.sh scripts, Makefile targets live
Governance [▓▓▓▓▓▓▓▓▓░]  90% - 3 specs; docs/ now has a runbook and a guide
Testing    [▓▓▓░░░░░░░]  30% - 4 Go modules, 7 test files, NONE ever compiled
```

Two honest caveats behind those bars:

- **Testing is 30% on file count, not on evidence.** `go` is not installed on this
  machine, so none of the seven test files has ever been compiled, let alone run.
  Treat them as written-but-unproven until `go mod tidy && go vet ./...` passes.
- **GCP and Azure are 40%, not more, because nothing has been applied.** The
  Terraform validates and the security properties are greppable, but with no CSP
  credentials no resource has ever been created.

---

## Active Work (WIP)

### In Progress

- None

### Blocked

- None

---

## Completed This Sprint ✅

- [x] `shared/bootstrap/` — image provisioner, home seeding, idle shutdown - 2026-08-18
- [x] `shared/packer/` — one template, three sources, all `packer validate` clean - 2026-08-18
- [x] `aws/shared/modules/vpc/` — implements `aws/tests/vpc_test.go`'s contract - 2026-08-18
- [x] `gcp/shared/modules/network/` and `azure/shared/modules/network/` (new) - 2026-08-18
- [x] `{aws,gcp,azure}/compute/dev-vm/` — brokered access, no public IP - 2026-08-18
- [x] `docs/runbooks/dev-vm-break-glass.md` and `docs/guides/dev-vm-git-access.md` - 2026-08-18
- [x] `.specify/003-dev-vm/` specification - 2026-08-18
- [x] Update `.opencode/command/cloud.provision.md` for the `--env`/`--workspace` split - 2026-08-18
- [x] Implement all 7 `scripts/cloud.*.sh` commands (SPEC-002) - 2026-08-18
- [x] Add `scripts/lib/common.sh` shared library - 2026-08-18
- [x] Write `scripts/README.md` CLI contract - 2026-08-18
- [x] Resolve committed merge conflict in `AGENTS.md` - 2026-08-18
- [x] Resolve committed merge conflict in `gcp/README.md` - 2026-08-18
- [x] Fix `.shellcheckrc` so per-file linting follows sourced libraries - 2026-08-18
- [x] Fix `TerraformDir` path in `aws/tests/vpc_test.go` - 2026-08-18
- [x] Verify SPEC-001 directory structure - 2026-08-18

Previous sprint (Governance Setup, 2026-02-07): `.opencode/command/` structure,
9 speckit commands, 7 CSP-aware cloud commands, `.specify/` structure,
`constitution.md`, multi-cloud `Makefile`, `AGENTS.md` references.

---

## Next Up (Top 3)

1. **Install the missing toolchain**, then re-run the gate. `go`, `tflint`,
   `tfsec`, `checkov`, `terraform-docs` and `infracost` are all absent, which is
   why five pre-commit hooks now sit on the manual stage and why no test file has
   ever been compiled. `aws/scripts/setup-dev.sh` installs them and treats tflint
   and tfsec as required. This unblocks the most verification for the least work.
2. **Remote state backends** (S3+DynamoDB / GCS / Azure Storage). Six Terraform
   roots across three clouds are on local state; `cloud.provision.sh` warns on
   every run and it is right to.
3. **Walk the day-one CSP checklist** in `.specify/003-dev-vm/quickstart.md` §6 on
   first credential access — nothing in SPEC-003 has been applied.

---

## Open Decisions

- **The three `vk/*` branches share no git history with `main`**
  (`git merge-base` exits 1), so they cannot be merged. They need to be either
  salvaged by cherry-picking content across, or deleted. Not yet decided.

---

## Known Gaps

| Gap | Detail |
|-----|--------|
| **Local toolchain** | `go`, `tflint`, `tfsec`, `checkov`, `terraform-docs`, `infracost` all missing. Five pre-commit hooks are on the manual stage because a missing binary exits 127 and blocks every `.tf` commit. |
| **Nothing applied** | No CSP credentials, so SPEC-003 is verified by `terraform validate` and grep only. No `plan`, no `apply`, no `packer build`. |
| **Tests uncompiled** | 7 Terratest files across 4 modules, none ever built. No `go.sum` in any `tests/` module. |
| Empty directories | root `tests/`, `examples/` (root `shared/` and `docs/` now have content) |
| No templates | `shared/templates/` absent, so `cloud.init.sh --template` has nothing to copy |
| Oracle | directory scaffolding only, no Terraform |
| Remote state | all six Terraform roots on local state |
| Budget alerts | scoped in SPEC-003, not implemented; NAT is ~$32/mo per cloud and does not stop when the VM does |
| Specs | `specs/001-csp-foundations/` is empty |
| Terraform fmt | 3 files from the initial commit still fail `terraform fmt -check` |
| Cost reporting | normalised table for AWS only; other providers pass native CLI output through |

---

## Metrics

| Category | Target | Actual | Status |
|----------|--------|--------|--------|
| Speckit Commands | 9 | 9 | ✅ |
| Cloud Commands (docs) | 7 | 7 | ✅ |
| Cloud Commands (scripts) | 7 | 7 | ✅ |
| Constitution | 1 | 1 | ✅ |
| Makefile | 1 | 1 | ✅ |
| Committed conflict markers | 0 | 0 | ✅ |
| Specs | 3 | 3 | ✅ |
| Terraform roots passing `validate` | all | 6/6 new | ✅ |
| Packer sources passing `validate` | 3 | 3 | ✅ |
| Clouds with Terraform | 4 | 3 | ⚠️ |
| Go test modules | 4+ | 4 | ⚠️ |
| Go test files that compile | 7 | **unknown** | ❌ |
| Terraform roots on remote state | 6 | 0 | ❌ |

`.tf` file counts: aws 101, gcp 9, azure 9, oracle 0.

"Go test modules 4" is a file-count metric and marked ⚠️ rather than ✅ on
purpose — `go` is not installed, so the modules exist but have never been built.
